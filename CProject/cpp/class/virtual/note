1，static成员变量
   初始化在类外进行
   不属于单独的某个对象，是所有对象共享
   先于对象存在
   若为public，则可以通过类名直接访问
   它和全局变量的区别？

2，static成员函数
   没有隐藏的this指针
   不能直接访问非static成员变量
   不能直接访问非static成员函数
   若为public，则可以通过类名直接访问

3，单例模式

4，const成员变量
   属于独立的对象
   必须在初始化列表中初始化

5，const成员函数
   隐藏的this指针为const类型
   不能直接修改非static成员变量
   能直接修改static成员变量
   只能调用static成员函数和const成员函数

6，const对象
   只能调用static成员函数和const成员函数

7，



